#! /usr/bin/env node

var build = require('manuscript-builder')

build({
  target: '/book.md',
  bookDir: '/chapters/',
  tableOfContents: '/chapters/Book.txt'
}, function () {
  console.log('book.md updated')
})
